const articleModel = require('../../models/article-model');

const expect = require('chai').expect;

describe('Test',function () {
   // let title = "test";
    //let date = "test";
    
    //let article = new Article(title, date, articleUrl, imageUrl, summary, content);


    it('1 should equal 1',function () {
        expect(1).to.equal(1);
    });
});
